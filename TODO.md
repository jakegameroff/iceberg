# TODO

- [ ] `iceberg server <url>` — set a custom server, overriding the default
      `https://iceberg.sh` (writes `server` in the config). With no argument,
      print the current one. The port is part of the URL
      (e.g. `iceberg server http://localhost:8080`), so no separate port setting.
